swagger: "2.0"
info:
  title: Service
  version: 1.0.0
schemes:
  - http
basePath: /
consumes:
  - application/json
produces:
  - application/json

paths:
  /template:
    get:
      operationId: template
      tags: [template]
      responses:
        200:
          description: Successful response
